# trace61_fp32_restart: Continue trace61 training with pure FP32 tFold (no AMP)
#
# Base: trace61_dynamic_pool checkpoint (latest.pt, ~2M steps)
# Change: Pure FP32 tFold scoring (no BF16/AMP wrapper)
# Goal: More accurate affinity scores → better specificity

total_timesteps: 2_000_000
n_envs: 8
n_steps: 32
batch_size: 256
n_epochs: 4
learning_rate: 1.5e-4
gamma: 0.98
gae_lambda: 0.95
clip_range: 0.2
entropy_coef: 0.012
vf_coef: 0.5
max_grad_norm: 0.5

max_tcr_len: 20
min_tcr_len: 8
max_pep_len: 25
max_steps_per_episode: 8
max_steps: 8
hla_pseudoseq_len: 34
ban_stop: true
terminal_reward_only: true
use_biochem_features: false  # Set to true to add 8D charge+hydrophobicity features

hidden_dim: 512
esm_model: "esm2_t33_650M_UR50D"
esm_frozen: true
esm_device: "cuda"
esm_tcr_cache_size: 4096
encoder: "esm2"

# Same reward as trace61
reward_mode: "v2_simple_target_gated_decoy"
use_delta_reward: false
w_affinity: 1.0
w_decoy: 0.3
w_naturalness: 0.05
w_diversity: 0.02

# Gate and bonus (same as trace61)
target_affinity_gate: -0.5
target_affinity_bonus: 1.0
decoy_affinity_penalty_weight: 0.3

# FP32 tFold (no AMP wrapper)
affinity_model: "tfold"
tfold_server_socket: "/tmp/tfold_server_trace61_fp32_restart.sock"
tfold_cache_path: "data/tfold_cache_trace61_fp32_restart.db"

# Decoy settings (same as trace61)
decoy_K: 2
n_contrast_decoys: 0
contrastive_agg: "mean"
decoy_tau: 10.0
decoy_tier_weights: {A: 0, B: 0, D: 0, C: 0}
decoy_unlock_schedule: {}
decoy_difficulty: "easy"
decoy_difficulty_schedule: {}

naturalness_scorer_type: "ae_gmm"  # Options: "esm2" (default), "ae_gmm"
naturalness_threshold_zscore: -2.0
cdr3_ppl_stats: "data/cdr3_ppl_stats.json"
# AE+GMM naturalness scorer (alternative to ESM-2)
naturalness_ae_model: "/share/liuyutian/TCRPPO/code/reward/ae_model"
naturalness_gmm_model: "/share/liuyutian/TCRPPO/code/reward/gmm.pkl"
naturalness_blosum: "/share/liuyutian/TCRPPO/code/blosum.txt"
naturalness_ae_threshold: 0.8  # combined (edit_acc + gmm_like) threshold
diversity_buffer_size: 512
diversity_similarity_threshold: 0.85

# Pure L2 base (50% from start, will mix with online pool)
l0_mutation_range: [3, 5]
l1_top_k: 500
curriculum_schedule:
  - {until: null, L0: 0.0, L1: 0.0, L2: 1.0}

# Online pool with dynamic band selection
online_tcr_pool_enabled: true
online_tcr_pool_start_step: 0  # Already warmed up from trace61
online_tcr_pool_warmup_steps: 1
online_tcr_pool_max_ratio: 0.5  # 50-50 L2 vs online pool
online_tcr_pool_max_per_target: 256
online_tcr_pool_min_affinity: -10.0
online_tcr_pool_max_decoy_violation: 999.0
online_tcr_pool_min_hamming: 2
online_tcr_pool_mutate_prob: 0.0

# Dynamic band selection
online_tcr_pool_use_dynamic_bands: true
online_tcr_pool_recent_window: 50

target_sampling: "uniform"
train_targets: "data/tfold_excellent_peptides.txt"

eval_interval: 100000
eval_n_tcrs: 5
eval_n_decoys: 0
eval_abort_threshold: 0.40
eval_warmup: 100000

checkpoint_interval: 20000
latest_checkpoint_interval: 2000
milestones: [100000, 200000, 300000, 400000, 500000, 600000, 700000, 800000,
  900000, 1000000, 1200000, 1400000, 1600000, 1800000, 2000000]

use_znorm: false
